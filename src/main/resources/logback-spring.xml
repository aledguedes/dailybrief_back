<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- ===================== -->
    <!-- 🔧 PROPRIEDADES GLOBAIS -->
    <!-- ===================== -->
    <property name="LOG_DIR" value="logs"/>
    <property name="LOG_FILE" value="${LOG_DIR}/dailybrief.log"/>
    <property name="MAX_FILE_SIZE" value="10MB"/>
    <property name="MAX_HISTORY" value="7"/>
    <property name="TOTAL_SIZE_CAP" value="100MB"/>

    <!-- ===================== -->
    <!-- 🖥️ CONSOLE APPENDER -->
    <!-- ===================== -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %cyan(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================== -->
    <!-- 📁 FILE APPENDER COM ROTAÇÃO -->
    <!-- ===================== -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- log-2025-05-30.0.log -->
            <fileNamePattern>${LOG_DIR}/dailybrief.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================== -->
    <!-- 🪵 NÍVEIS DE LOG -->
    <!-- ===================== -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.hibernate.SQL" level="DEBUG"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>
    <logger name="com.aledguedes" level="DEBUG"/>

    <!-- ===================== -->
    <!-- 🌐 ROOT LOGGER -->
    <!-- ===================== -->
    <root level="INFO">
        <appender-ref ref="Console"/>
        <appender-ref ref="RollingFile"/>
    </root>

</configuration>
